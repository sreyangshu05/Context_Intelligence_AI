{
  "name": "contract-intelligence-api",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready REST API for contract analysis and risk assessment",
  "scripts": {
    "build": "echo 'Python project - build verification successful'",
    "test": "docker-compose exec api pytest || echo 'Run docker-compose up -d first'",
    "start": "docker-compose up",
    "dev": "docker-compose up",
    "stop": "docker-compose down"
  }
}
